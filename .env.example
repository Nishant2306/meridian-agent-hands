# Copy to .env and fill in. .env is gitignored and must never be committed.
# Never place real API keys, real credentials, or real PII in this file.

ANTHROPIC_API_KEY=
LLM_MODEL=
HEADLESS=false
LOG_LEVEL=info
OPERATOR_PORT=4190
